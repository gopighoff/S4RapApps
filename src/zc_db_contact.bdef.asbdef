projection implementation in class ZBP_C_DB_CONTACT unique;
strict ( 2 );
use draft;
define behavior for ZC_DB_CONTACT alias ZcDbContact
use etag

{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}