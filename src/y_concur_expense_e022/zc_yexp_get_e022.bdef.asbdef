projection;
strict ( 2 );
use draft;
use side effects;

define behavior for ZC_YEXP_GET_E022
use etag
{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;

  use action Retrieve_Report;
  use action Refresh;
  use action Post_Invoice;
}